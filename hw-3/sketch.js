// declared the setup funtion for the sketch
function setup() {

// created a canvas 600 by 400 pixels
  createCanvas(600, 400);

// made the background color pink
  background( 'pink' );
}

// declared the draw() function for sketch
function draw(){

// used the text() function to display "Hello World!"
  text("Hello World!", 300, 200);
}
